[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "gvpy"
version = "0.0.1"
description = "Integrated glaciovolcanic eruption model in python"
authors = [
  { name = "Colin R. Rowell" },
]
license = { file = "LICENSE" }
readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License"
]
dependencies = [
    "ipython",
    "jupyterlab",
    "loguru",
    "matplotlib",
    "mkdocs",
    "notebook",
    "numpy",
    "pandas",
    "pip",
    "pytest",
    "python-dotenv",
    "ruff",
    "scikit-learn",
    "scipy",
    "tqdm",
    "typer",
    "xarray",
]
requires-python = "~=3.13.0"


[tool.ruff]
line-length = 99
src = ["gvpy"]
include = ["pyproject.toml", "gvpy/**/*.py"]

[tool.ruff.lint]
extend-select = ["I"]  # Add import sorting

[tool.ruff.lint.isort]
known-first-party = ["gvpy"]
force-sort-within-sections = true

